# Copy: cp .env.example .env.local

STORY_DATABASE_URL=postgresql://betterbox:betterbox@127.0.0.1:54329/betterbox_story
PORT=3001

# Link "Mở trên reader" từ admin UI
NEXT_PUBLIC_STORY_READER_URL=http://localhost:3000

# Set false khi truy cập admin qua HTTP trên LAN (giống story_reader)
# COOKIE_SECURE=false

# Production: restrict admin UI to VPN/trusted network (container has docker.sock access).
